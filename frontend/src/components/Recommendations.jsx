import MovieCard from './MovieCard'
export default function Recommendations({ results, loading, error, selectedMovie }) { if (loading) return <section className="state"><span className="spinner" /> Building your watchlist…</section>; if (error) return <section className="state error">{error}</section>; if (!selectedMovie) return <section className="state subtle">Select a movie from the dropdown to get recommendations.</section>; if (!results) return <section className="state subtle">Choose “Recommend movies” to build your watchlist.</section>; if (!results.recommendations?.length) return <section className="state subtle">No similar movies were found for this title.</section>; return <section className="results"><div><p className="eyebrow">YOUR MATCHES</p><h2>Because you liked {results.selected_movie}</h2></div><div className="movie-grid">{results.recommendations.map(movie => <MovieCard key={movie.title} movie={movie} />)}</div></section> }
